import { SnippetMeta } from "../../../types/meta";
import fieldMeta from "./field";
import wrapperMeta from "./wrapper";
import providerMeta from "./provider";

export default [providerMeta, wrapperMeta, fieldMeta] as SnippetMeta[];
